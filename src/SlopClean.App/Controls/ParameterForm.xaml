<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="SlopClean.App.Controls.ParameterForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SlopClean.App.ViewModels">
    <Border
        Padding="12"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        CornerRadius="8">
        <ItemsRepeater ItemsSource="{x:Bind Items, Mode=OneWay}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="vm:ParameterItemViewModel">
                    <Grid Margin="0,0,0,12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind DisplayName}" />
                            <TextBlock
                                Opacity="0.75"
                                TextWrapping="Wrap"
                                Text="{x:Bind Description}" />
                        </StackPanel>
                        <ToggleSwitch
                            Grid.Column="1"
                            IsOn="{x:Bind BoolValue, Mode=TwoWay}"
                            Visibility="{x:Bind IsBool, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <NumberBox
                            Grid.Column="1"
                            Minimum="0"
                            SpinButtonPlacementMode="Inline"
                            Value="{x:Bind IntValue, Mode=TwoWay}"
                            Visibility="{x:Bind IsInt, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <ComboBox
                            Grid.Column="1"
                            ItemsSource="{x:Bind EnumValues}"
                            SelectedItem="{x:Bind StringValue, Mode=TwoWay}"
                            Visibility="{x:Bind IsEnum, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Grid>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </Border>
</UserControl>
