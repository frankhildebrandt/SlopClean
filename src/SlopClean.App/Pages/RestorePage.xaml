<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SlopClean.App.Pages.RestorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SlopClean.App.Controls">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Restore" />
            <TextBlock TextWrapping="Wrap" Text="Restore data from backups created when applying cleanup actions." />
            <TextBlock Opacity="0.75" Text="{x:Bind ViewModel.BackupPathText, Mode=OneWay}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button
                Command="{x:Bind ViewModel.RestoreSelectedCommand}"
                Content="Restore selected"
                Style="{StaticResource AccentButtonStyle}" />
            <Button Command="{x:Bind ViewModel.RefreshCommand}" Content="Refresh" />
            <Button Command="{x:Bind ViewModel.CancelCommand}" Content="Cancel" />
        </StackPanel>

        <controls:RestorePointList
            Grid.Row="3"
            Items="{x:Bind ViewModel.Items}" />

        <controls:ScanProgressControl
            Grid.Row="4"
            IsBusy="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
            StatusText="{x:Bind ViewModel.StatusText, Mode=OneWay}" />
    </Grid>
</Page>
